#include <main.h>

/**
 * main - entry point
 *
 * Return: Always 0 (success)
 */
int add(int, int);

int main(void)
{
	int n;

	n = add(89, 9);
	_putchar("%d\n", n);

	return (0);
}

int add(int, int)
{
	int result = int + int;

	return (result);
}
