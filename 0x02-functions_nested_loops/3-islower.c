#include <main.h>

/**
 * main - entry point
 *
 * Description: Function that checks for lowercase charactors
 */
int _islower(int c)
{
	int result;

	if (c == 99)
	{
		result = 1;
		return (result);	
	} else {
		result = 0;
		return (result);
	}
}

